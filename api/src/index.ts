/**
 * @doc-platform/api
 * Backend API server.
 */

const PORT = process.env.PORT ?? 3000;

console.log(`API server starting on port ${PORT}...`);

// Placeholder - will be replaced with Express/Fastify server
